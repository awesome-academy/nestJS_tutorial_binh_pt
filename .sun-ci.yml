workspace: true

stages:
- build
- test

jobs:
  build:
    stage: build
    image: node:12-alpine
    script:
    - npm install
    - npm run build

  test:
    stage: test
    image: node:12-alpine
    script:
    - npm run lint
    - npm run test
